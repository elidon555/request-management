package com.example.project.services.request.updateRequest;

import com.example.project.mapper.RequestDTO;


public interface UpdateRequest {
    RequestDTO updateRequest(RequestDTO requestDTO);
}
