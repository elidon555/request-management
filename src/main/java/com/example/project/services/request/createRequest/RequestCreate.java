package com.example.project.services.request.createRequest;

import com.example.project.mapper.RequestDTO;


public interface RequestCreate {
    RequestDTO createRequest(RequestDTO requestDTO);
}
